# Nav2 런치 전용 override: 소셜 레이어 OFF.
# 계층 원칙 — 플래너(MPPI, footprint 회피)가 횡제어를 소유할 때 소셜
# 레이어까지 켜면 두 횡제어기가 충돌 (스모크 실측: YIELD/STOP 반복,
# 밴드 가장자리 표류). 게이트는 안전 본연(STOP/SLOW/fast-class/blind-hold)
# 역할만. teleop/직진 러너 경로에서는 소셜 ON 유지.
safety_stop_node:
  ros__parameters:
    social_enable: false
